meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17
build:
  artifacts:
    target/*.jar: /
run:
  jarName: target/DiscordjBot-1.0-jar-with-dependencies.jar
  persistenceMount: /data
  containerPort: 80